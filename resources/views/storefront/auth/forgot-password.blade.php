@extends('layouts.storefront')
@section('content')<div class="sf-container sf-auth-page"><section class="sf-auth-card"><h1>{{ __('storefront.forgot_password') }}</h1><p>{{ __('storefront.forgot_password_text') }}</p><form method="post" action="{{ route('password.email') }}" class="sf-auth-form">@csrf<label>{{ __('storefront.email') }}<input type="email" name="email" value="{{ old('email') }}" required>@error('email')<small>{{ $message }}</small>@enderror</label><button class="sf-btn" type="submit">{{ __('storefront.send_reset_link') }}</button></form></section></div>@endsection
